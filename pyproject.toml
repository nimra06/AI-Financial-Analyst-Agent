[project]
name = "ai-financial-analyst-api"
version = "0.7.0"
requires-python = ">=3.11"

[tool.vercel]
entrypoint = "app.api.main:app"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["app"]
python_files = ["test_*.py"]

[tool.ruff]
line-length = 100
target-version = "py311"
